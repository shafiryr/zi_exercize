import { GetMemberIdPipe } from './get-member-id.pipe';

describe('GetMemberIdPipe', () => {
  it('create an instance', () => {
    const pipe = new GetMemberIdPipe();
    expect(pipe).toBeTruthy();
  });
});
